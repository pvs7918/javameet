package seminar5.lesson;

public class howto {
    /*для сведения
    
    //Collections.sort(list, (o1, o2) -> o1.getValue().compareTo(o2.getValue())); - сортировка по возрастанию значения
    //Collections.sort(list, (o1, o2) -> o2.getValue().compareTo(o1.getValue())); - сортировка по убыванию значения
*/
}
